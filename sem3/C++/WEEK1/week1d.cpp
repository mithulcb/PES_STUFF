#include <iostream>
#include<stdlib.h>
using namespace std;
int main() {
   int length, area;
   cout << "\nEnter the Length of Square : ";
   cin>>length;
   area = length * length;
   cout << "\nArea of Square : " << area;
   return (0);
}